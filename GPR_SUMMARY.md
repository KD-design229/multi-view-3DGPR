# 探地雷达 (GPR) 数据处理与模型适配总结

## 1. 探地雷达数据处理 (Data Processing)

该库通过 `code/data.py` 和 `code/data_augmention_loader_ori.py` 等文件实现了针对 GPR 数据的加载、预处理和增强。

### 多视图数据配对与加载
*   **配对逻辑**: 针对 GPR 数据通常包含不同视图（如 B-scan 侧视图和 C-scan 顶视图）的特性，代码实现了严格的文件名匹配逻辑（如 `find_indices_both_views` 函数），将同一检测点的“顶视图”（Top View, `_h.png`）与“主视图”（Main View, `_v.png`）进行配对加载。
*   **标签映射**: 通过文件夹名称（如 "0_双曲线", "1_暗斑", "2_裂缝" 等）识别图像对应的地下病害类型，并在加载时将两个视图的标签融合为最终的病害分类标签（`get_problem` 函数）。

### GPR 特有的数据增强
*   **噪声模拟**: 针对雷达信号易受干扰的特点，实现了 `AddPepperNoise` (椒盐噪声) 和 `Gaussian_noise` (高斯噪声) 类，用于模拟地下介质不均匀性或设备噪声。
*   **缺失模拟**: 实现了 `Cutout` 和 `HidePatch` 类，随机遮挡图像块，模拟信号丢失或局部数据不完整的情况。
*   **常规增强**: 使用 `torchvision` 进行随机翻转、亮度/对比度调整等，增加模型鲁棒性。

### 类别不平衡处理
*   实现了 `up_sample` 函数，通过随机复制少数类样本来解决地下病害样本分布不均的问题。

## 2. 针对 GPR 特性的代码模型适配 (Model Adaptations)

该库在 `models/densenet_MVFD.py` 中实现了专门的 **FusionDenseNet** 模型，并在 `code/train_DenseNet121_MVFD.py` 中实现了相应的训练策略，以适应 GPR 的多视图特性。

### 双分支网络架构 (Dual-Branch Architecture)
*   模型包含两个独立的 `DenseNet` 分支 (`dn1` 和 `dn2`)，分别专门处理顶视图和主视图数据。这允许网络分别学习不同视图下的雷达波形特征（如双曲线特征与平面纹理特征）。

### 图注意力融合机制 (Graph Attention Fusion)
*   引入了图注意力网络（GAT）模块 (`GAT` 和 `Attention` 类)。在特征提取的末端，通过 GAT 动态计算两个视图特征的权重 (`beta_x`, `beta_y`)。
*   这种机制允许模型根据图像质量或特征显著性，自动“关注”更具信息量的视图（例如，当主视图信号模糊时更多依赖顶视图），从而实现自适应的特征融合。

### 切换式在线知识蒸馏 (SwitOKD)
*   在训练脚本 (`train_DenseNet121_MVFD.py`) 中，通过复杂的条件逻辑（计算 `epsilon`, `delta` 等指标），实现了三个输出头（顶视图、主视图、融合视图）之间的相互监督。
*   利用 KL 散度 (`kl_div`) 作为蒸馏损失，让表现较好的分支/视图“指导”表现较差的分支，从而利用多视图之间的互补信息提升整体性能。
